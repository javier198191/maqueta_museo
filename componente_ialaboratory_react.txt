import React, { useState } from 'react';

/**
 * Componente: IALaboratory
 * Basado en {{DATA:SCREEN:SCREEN_6}}
 */
const IALaboratory = () => {
  const [selectedStyle, setSelectedStyle] = useState('Pop Art');
  const [imagePreview, setImagePreview] = useState('https://images.unsplash.com/photo-1544005313-94ddf0286df2?auto=format&fit=crop&q=80&w=600');

  const styles = [
    { name: 'Pop Art', icon: 'star' },
    { name: 'Expresionismo', icon: 'brush' },
    { name: 'Caricatura', icon: 'sentiment_very_satisfied' },
    { name: 'Óleo', icon: 'opacity' }
  ];

  return (
    <div className="flex flex-col min-h-screen bg-mamb-white pb-24">
      {/* Header Interactivo */}
      <div className="p-8 text-center pt-12">
        <h1 className="text-5xl font-black text-gray-900 leading-tight">
          ¡Crea tu <br />
          <span className="text-caribbean-blue">Obra!</span>
        </h1>
        <p className="mt-4 text-gray-500 font-medium leading-relaxed">
          Sube una foto y deja que la magia del laboratorio la convierta en arte increíble.
        </p>
      </div>

      {/* Botón Subir */}
      <div className="px-6">
        <button className="w-full bg-caribbean-blue text-white py-6 rounded-mamb flex items-center justify-center gap-3 shadow-lg active:scale-95 transition-all text-xl font-bold">
          <span className="material-icons-outlined text-3xl">photo_camera</span>
          Subir Foto
        </button>
      </div>

      {/* Vista Previa */}
      <div className="mt-8 px-6 relative">
        <div className="bg-white p-4 rounded-mamb shadow-xl border-4 border-white">
          <div className="absolute top-8 left-10 z-10 bg-energetic-orange text-white px-4 py-1 rounded-full text-sm font-bold flex items-center gap-1 shadow-md">
            <span className="material-icons text-sm">auto_awesome</span>
            Vista Previa
          </div>
          <img 
            src={imagePreview} 
            alt="Obra en proceso" 
            className="w-full aspect-square object-cover rounded-[32px]"
          />
        </div>
      </div>

      {/* Selector de Estilos */}
      <div className="mt-8 px-6">
        <div className="bg-white rounded-[40px] p-6 shadow-lg">
          <div className="flex items-center gap-2 mb-6">
            <span className="text-energetic-orange text-2xl">palette</span>
            <h2 className="text-xl font-bold text-gray-800">Estilos Mágicos</h2>
          </div>

          <div className="grid grid-cols-2 gap-4">
            {styles.map((style) => (
              <button
                key={style.name}
                onClick={() => setSelectedStyle(style.name)}
                className={`flex items-center gap-3 p-4 rounded-3xl transition-all ${
                  selectedStyle === style.name 
                    ? 'bg-sky-100 border-2 border-caribbean-blue text-caribbean-blue' 
                    : 'bg-gray-100 text-gray-600'
                }`}
              >
                <div className={`p-2 rounded-full ${selectedStyle === style.name ? 'bg-caribbean-blue text-white' : 'bg-gray-200'}`}>
                  <span className="material-icons text-xl">{style.icon}</span>
                </div>
                <span className="font-bold">{style.name}</span>
              </button>
            ))}
          </div>

          <button className="w-full mt-8 bg-energetic-orange text-white py-4 rounded-full font-black text-lg shadow-lg active:scale-95 transition-all">
            ¡Aplicar Magia!
          </button>
        </div>
      </div>
    </div>
  );
};

export default IALaboratory;
